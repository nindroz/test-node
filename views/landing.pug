extends layout

block content
  h1= title
  p Wellcome to #{title}
  form(action='/', method='POST')
    input(type="email", name="submit_email", placeholder="Enter your email adress")
    button(type="submit") SUBMIT
